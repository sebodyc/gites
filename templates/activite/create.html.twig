{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <style>

        h2 {
            padding-bottom: 2rem;
            padding-top: 2rem;
        }
    </style>

    <div class="col-md-8">
        <p>teub</p>
        <h1>create</h1>
        {{ form_start(activiteForm) }}
        {{ form_widget(activiteForm) }}
        <button type="submit" class="btn btn-success">enregister les modif</button>
        {{ form_end(activiteForm) }}
    </div>
{% endblock %}
