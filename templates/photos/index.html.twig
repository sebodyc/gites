{% extends 'base.html.twig' %}

{% block title %}Hello PhotosController!{% endblock %}


{% block body %}
<style>
    .row {
        margin: 15px;
    }
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container">

    
    {% for photo in photos %}

    <div class="container">
        <div class="row">
            <a href="{{ asset('images/photos/') }}{{ photo.image }}" data-toggle="lightbox" data-gallery="gallery" class="col-md-4">
                <img src="{{ asset('images/photos/') }}{{ photo.image }}" class="img-fluid rounded">
            </a>
        </div>
    </div>
{#    <article class="activite">#}
{#        <div class="content">#}
{#            <h2>{{ photo.name }}</h2>#}
{#            <p>{{ photo.description }}</p>#}
{#        </div>#}
{#        {% if photo.image %}#}
{#        <div class="image">#}
{#            <img class="w-25 p-3" src="{{ asset('images/photos/') }}{{ photo.image }}" alt="{{ photo.name }}">#}
{#        </div>#}
{#        {% endif %}#}
    {% endfor %}

</div>
{% endblock %}
