{% extends 'base.html.twig' %}

{% block body %}
    <div class="container text-center mt-4 mb-4">
        <a class="btn btn-danger"   href="{{ path('mypics') }}"> <i class="fas fa-trash-alt"></i> Supprimer des photos</a>
    </div>
    <div class="container text-center mt-4 mb-4">
        <h1>OU</h1>
        <h1>Ajouter une photo</h1>
        <div class="row ">
            <div class="mx-auto">
        {{ form_start(photosForm) }}
        {{ form_widget(photosForm) }}
        <button type="submit" class="btn btn-success">enregister les modifs</button>
        {{ form_end(photosForm) }}
            </div>
        </div>
    </div>

{% endblock %}

{% block title %}

{% endblock %}
